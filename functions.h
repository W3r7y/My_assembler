#include "data.h"

char *skipSpacesAndTabs(char *p);

void *malloc_with_check(long size);

char *strallocat(char *str1, char* str2);

int printf_line_error(line curr_line, char *message, ...);

void my_itoa(int num, char str[]);

void reverse(char str[]);
